import React from "react";
import {Formik} from "formik";

export const AccountPrefFormContent = (props) => {
    const {
        status,
        values,
        errors,
        touched,
        dirty,
        isSubmitting,
        handleChange,
        handleBlur,
        handleSubmit,
        handleReset
    } = props;
    console.log(values)


    return ( //form here

    )

}